{
	"name": "@workspace/shared",
	"version": "1.0.0",
	"author": "arenarium.dev",
	"type": "module",
	"scripts": {
		"build:wasm": "zig build-lib wasm/states.zig -O ReleaseSmall -target wasm32-freestanding -dynamic -rdynamic -femit-bin=./wasm/output/states.wasm",
		"build:wasm:js": "zig run wasm/js.zig"
	}
}
